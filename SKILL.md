---
name: weekly-report
description: 生成实习生周报的 .md 和 .xlsx 文件。当用户说"生成周报"、"写周报"、"本周周报"、"这周周报"、"实习生周报"、"weekly report"或需要创建周报时触发。适用于任何需要按模板填写并输出 md + xlsx 双格式周报的场景。
---

# 周报生成

> 路径约定：文中 `assets/`、`scripts/` 等相对路径均相对于本 SKILL.md 所在目录（即技能目录）。本技能支持以 junction/符号链接安装到 `~/.claude/skills/`，执行任何命令前先确认该目录位置。

根据标准模板生成实习生周报，输出两个文件：
- `.md` 文件 — Markdown 格式，适合 Obsidian 知识库
- `.xlsx` 文件 — Excel 格式，用于正式提交

两份文件内容一致，`.md` 是内容源，`.xlsx` 从 `.md` 内容驱动填写。

---

## 流程总览

```
确认基本信息 → 确认日期 → 收集内容 → 生成 .md → 生成 .xlsx
```

严格按顺序执行，每步确认后再进入下一步。

---

## 第一步：收集基本信息

逐一询问以下信息（不要一次性全问，分两组）：

**第一组 — 人员信息：**
1. 姓名
2. 部门（具体到末级组织，如"财务开发组-应用软件开发"）
3. 岗位（如"实习生"）
4. 指导老师
5. 入职时间

**第二组 — 输出位置：**
6. 周报输出目录（用户未指定则默认当前工作目录）

将收集到的信息汇总展示，请用户确认无误后再继续。

---

## 第二步：确定日期与分表

### 日期计算

根据当前日期自动计算，展示给用户确认：

- **周报日期范围**：本周周一 ~ 本周周日（格式 `YYYYMMDD-YYYYMMDD`）
- **填写日期**：本周周五（格式 `YYYY-MM-DD`）

> 用户可手动调整日期范围。若用户提供的日期不是周一，自动修正为该周周一。

### 分表选择（重要：不要自己算）

xlsx 模板内含多个分表（sheet），**不要自行决定填哪个**。正确做法：

1. **先读取模板**：用 Python/openpyxl 读取 `assets/实习生周报标准模板.xlsx` 的 `sheetnames`
2. **列出选项给用户**：将现有分表名称一一列出
3. **询问用户**："模板包含以下分表：[列出名称]。本周周报应填入哪个分表？如需新建分表，请告知新分表的名称（如「9月第1周」）"
4. **若用户选择新建**：脚本 `fill_weekly_report.py` 会自动从现有分表复制结构并命名（`_get_or_create_sheet` 已实现此功能）

将日期范围和用户选择的分表名称汇总确认后再继续。

---

## 第三步：收集周报内容

询问用户本周工作内容来源。常见方式（让用户选择）：

- **A. 口头描述**：用户口述，你整理填入四个板块
- **B. 指定文件/笔记**：用户给出文件路径，你从中提取
- **C. 交互式问答**：你逐板块提问，用户回答

### 四个板块

| 板块 | 格式 | 说明 |
|------|------|------|
| 一、本周工作内容 | 表格，序号+工作内容+完成情况 | 按任务逐条列出，每条约1-2行 |
| 二、学习与收获 | 编号列表，每条1-3句 | 新知识、新技能、新方法、新认识 |
| 三、问题与困难 | 编号列表，每条1-3句 | 困难、未解决问题、需协调事项 |
| 四、下周工作计划 | 编号列表，每条1句 | 下周拟开展的主要工作 |

### 内容质量要求（严格遵守）

这是周报的灵魂 —— **空洞的周报等于没写**。生成前逐条自检：

**禁止项（绝对不允许出现）：**
- ❌ "无" — 如果本周确实无事可写，写明原因（如"本周因XX培训未参与开发"）
- ❌ "正常""顺利" — 这类词单独出现等于废话，必须说明**怎么正常、怎么顺利**
- ❌ "完成了XX任务" — 太笼统，必须写清楚**怎么做的、用了什么方法、产出是什么**
- ❌ "学习了XX技术" — 必须写**学到了什么具体知识、能解决什么问题**
- ❌ "继续XX工作"（下周计划） — 必须写**下周具体要推进到什么程度、交付什么**

**每一项自问三个问题：**
1. 一个不了解我工作的人读完这条，能说清楚我做了什么吗？
2. 这条里有没有具体的技术名词、文件名、数据或时间节点？
3. 这条的「完成情况」是否描述了可验证的结果（如"已提交至XX"、"已部署验证通过"）？

**各板块数量建议：**
- 工作内容表格：3~5 条，少于 3 条说明本周产出不足，需要在学习与收获中补充
- 学习与收获：3~5 条
- 问题与困难：1~4 条（如实填写，没有困难也要写"无重大困难"，并简要说明原因）
- 下周计划：3~5 条

**条目数量灵活，模板预留行数不是上限：**
模板中任务表格通常预留 5 行、其余板块各预留 4 行（用户也可能自己改过预留数），这只是初始预留。实际条目可多可少：
- 内容多于预留行 → xlsx 生成时自动插入新行（保留样式与合并单元格）
- 内容少于预留行 → 自动删除空白行，末尾说明文字跟随上移

因此收集内容时**不要受"模板只有 4/5 行"限制**，按实际工作如实列出即可。

---

## 第四步：生成 .md 周报

### 文件命名

```
姓名工作周报(YYYYMMDD-YYYYMMDD)-指导老师.md
```

### 文件结构

使用以下精确模板：

```markdown
---
type: 周报
tags:
  - 周报
created: YYYY-MM-DD
---

# 姓名工作周报（YYYYMMDD-YYYYMMDD）-指导老师

> 实习生周报
> 姓名：[姓名]    部门：[部门]    岗位：[岗位]
> 指导老师：[指导老师]    入职时间：[入职时间]    填写日期：[填写日期]

---

## 一、本周工作内容（按任务逐条列出，说明工作内容、完成情况）

| 序号 | 工作内容 | 完成情况 |
|------|----------|----------|
| 1 | [内容] | [情况] |

---

## 二、学习与收获（本周学到的新知识、新技能、新方法，或对岗位/业务的新认识）

1、[内容]

---

## 三、问题与困难（工作中遇到的困难、尚未解决的问题，或需要指导老师/部门协调的事项）

1、[内容]

---

## 四、下周工作计划（逐条列出下周拟开展的主要工作）

1、[内容]
```

### 生成后自检

生成 .md 后，**先对照第三步的质量要求逐项自检**，发现空洞条目立即要求用户补充，不要带着不合格内容进入 xlsx 生成环节。

自检通过后展示给用户审阅，允许修改后再进入第五步。

### 注意事项

- frontmatter 中 `type: 周报`，`created` 填填写日期
- wikilink 使用 `[[路径/文件名.md]]` 格式（含 `.md` 扩展名）
- 工作内容表格中，序号列、工作内容列（B+C 合并）、完成情况列（D+E+F 合并），与 xlsx 合并单元格对应

---

## 第五步：生成 .xlsx 周报

### 前置检查

检查当前环境是否具备 `xlsx` skill：
- 如果 xlsx skill 可用 → 优先使用 xlsx skill 填写
- 如果不可用 → 使用本技能自带的 `scripts/fill_weekly_report.py` 脚本

如果用户本地既无 xlsx skill 也无 openpyxl（Python 库），引导用户安装：
```
pip install openpyxl
# 或
uv pip install openpyxl
```
若用户不想安装，可退而求其次：手动打开模板 xlsx 填写。

### 使用自带脚本

```bash
python scripts/fill_weekly_report.py \
  --template assets/实习生周报标准模板.xlsx \
  --output <输出路径>/姓名工作周报(YYYYMMDD-YYYYMMDD)-指导老师.xlsx \
  --name "姓名" \
  --department "部门" \
  --position "岗位" \
  --mentor "指导老师" \
  --entry-date "入职时间" \
  --fill-date "填写日期" \
  --sheet "8月第3周" \
  --tasks '[["内容1","完成情况1"],["内容2","完成情况2"]]' \
  --learnings '["收获1","收获2"]' \
  --problems '["问题1","问题2"]' \
  --plans '["计划1","计划2"]'
```

脚本会自动：
1. 复制模板到输出路径
2. 定位或创建正确的分表
3. 填写基本信息（姓名、部门、岗位、指导老师、入职时间、填写日期）
4. 按表头文本动态定位四个板块，填写内容
5. 自动调整条目行数：内容多于模板预留行 → 在板块末尾插入新行（复制样式与合并单元格）；内容少于预留行 → 删除空白行，说明文字跟随移动

### xlsx 模板结构说明

模板位于 `assets/实习生周报标准模板.xlsx`，结构如下（条目行数为初始预留值，实际行数由脚本按内容自动调整）：

| 区域 | 内容 | 合并单元格 |
|------|------|------------|
| 标题行 | 标题"实习生周报" | A:F |
| 信息行 | 姓名(B) / 部门(D) / 岗位(F)；指导老师(B) / 入职时间(D) / 填写日期(F) | — |
| 一、本周工作内容 | 表头行 + 列头行（序号/工作内容/完成情况）+ 预留 5 行条目 | 条目行 B:C 合并、D:F 合并 |
| 二、学习与收获 | 表头行 + 预留 4 行条目 | 条目行 A:F 合并 |
| 三、问题与困难 | 表头行 + 预留 4 行条目 | 条目行 A:F 合并 |
| 四、下周工作计划 | 表头行 + 预留 4 行条目 | 条目行 A:F 合并 |
| 末尾 | 说明文字 | A:F 合并 |

脚本需处理的要点：
- 板块位置按表头文本动态识别，不依赖固定行号（用户改动模板预留行数也能正常工作）
- 合并单元格区域在写入前保留，写入内容后保持合并
- 分表名称不存在时，复制现有分表结构并重命名
- 条目行数随内容自动增减：多了在板块末尾插入行（复制样式与合并），少了删除空白行
- 日期格式使用 `yyyy/m/d`（与模板一致）

---

## 完成后

1. 告知用户两个文件的完整路径
2. 提醒用户："md 版为内容源，xlsx 用于正式提交，请检查后提交至指导老师"
3. 如果用户项目中有 MOC 主页，提醒更新周报索引表格
