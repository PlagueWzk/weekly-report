---
name: weekly-report
description: 生成实习生周报的 .md 和 .xlsx 文件。当用户说"生成周报"、"写周报"、"本周周报"、"这周周报"、"上周周报"、"补写周报"、"实习生周报"、"weekly report"或需要创建周报时触发；也用于"日报/报工"速记（git 只读法，见正文附节）。周报与日报的内容收集都支持「git 只读提取」来源：用户给出项目路径时先探测是否为 git 仓库，锁定本周/当天日期只读查询提交与 diff 汇总（绝不 add/commit/push/checkout 等写操作、不要求先提交），git 反映不出的部分再向用户询问补齐。适用于任何需要按模板填写并输出 md + xlsx 双格式周报、或需要按 git 提交记录生成日报/报工的场景。
---

# 周报生成

> 路径约定：文中 `assets/`、`scripts/`、`profile.json` 等相对路径均相对于本 SKILL.md 所在目录（即技能目录），执行任何命令前先确认该目录位置。

根据标准模板生成实习生周报，输出两个文件：
- `.md` 文件 — Markdown 格式，适合 Obsidian 知识库
- `.xlsx` 文件 — Excel 格式，用于正式提交

两份文件内容一致，`.md` 是内容源，`.xlsx` 从 `.md` 内容驱动填写。

**材料来源支持 git 只读提取**：用户给出项目路径时，锁定日期范围（周报锁本周、报工锁当天），只读查询该项目的提交与 diff 汇总成素材。全程只用查询命令，**绝不 add/commit/push/checkout/stash**，也不要求用户先提交（未提交改动同样会读取并如实标注）。细则见第三步 D，按天输出的日报/报工见文末附节。

---

## 流程总览

```
读取/收集基本信息 → 确认日期与分表 → 收集内容（可选：D. 锁定日期只读查 git 提交/diff）→ 对照上周计划 → 生成 .md → 生成 .xlsx
```

严格按顺序执行，每步确认后再进入下一步。

---

## 第零步：个人信息档案（profile.json）

个人信息保存在技能目录下的 `profile.json`（已加入 .gitignore，不会被提交），避免每周重复询问。

**每次运行时：**

1. 检查技能目录下是否存在 `profile.json`
2. **存在** → 读取并展示全部字段，询问用户："检测到已保存的个人信息如下，是否沿用？需要修改请直接说明"。用户要求修改时，更新对应字段并**重写整个 profile.json**（UTF-8）
3. **不存在** → 进入第一步收集，确认后保存

**文件结构（保存时严格遵守）：**

```json
{
  "name": "姓名",
  "department": "部门（具体到末级组织）",
  "position": "岗位",
  "mentor": "指导老师",
  "entry_date": "YYYY-MM-DD",
  "output_dir": "周报默认输出目录，空字符串表示用当前工作目录"
}
```

> 可选字段 `git_author`：git 提交作者名。当姓名与 git 作者不一致、或按姓名匹配不到提交时填写；未填写时，git 来源（第三步 D）先按姓名匹配，匹配不到再询问用户。

保存/更新时告知用户："个人信息已保存到技能目录的 profile.json（已 gitignore，不会提交），之后说「更新周报个人信息」即可修改。"

---

## 第一步：收集基本信息

若 `profile.json` 已存在且用户确认沿用 → 跳过本步（输出目录也取档案中的 `output_dir`）。

否则逐一询问以下信息（不要一次性全问，分两组）：

**第一组 — 人员信息：**
1. 姓名
2. 部门（具体到末级组织，如"财务开发组-应用软件开发"）
3. 岗位（如"实习生"）
4. 指导老师
5. 入职时间

**第二组 — 输出位置：**
6. 周报输出目录（用户未指定则默认当前工作目录）

将收集到的信息汇总展示，请用户确认无误后**写入 `profile.json`** 再继续。

---

## 第二步：确定日期与分表

### 日期计算（智能默认）

根据"今天是星期几"给出默认值，展示给用户确认（用户可随时调整）：

| 今天是 | 默认周报范围 | 默认填写日期 |
|--------|--------------|--------------|
| 周二 ~ 周五 | 本周一 ~ 本周日 | 本周五 |
| 周六 / 周日 / 周一 | 上周一 ~ 上周日 | 上周五 |

> 周报提交时限为周五 20:00，周一与周末补写的通常是刚结束的那一周。

补充规则：
- 周报日期范围格式 `YYYYMMDD-YYYYMMDD`，填写日期格式 `YYYY-MM-DD`
- 用户手动指定日期 → 以用户为准；若指定日期不是周一，自动修正为该周周一，填写日期取该周周五

### 分表选择（重要：不要自己算）

xlsx 模板内含分表（sheet），**不要自行决定填哪个**。正确做法：

1. **先读取模板**：用 Python/openpyxl 读取 `assets/实习生周报标准模板.xlsx` 的 `sheetnames`
2. **列出并建议**：将现有分表名称一一列出，同时根据周报范围自动建议分表名（周一所在月份 + 当月第几周，如 9 月第一周 → 「9月第1周」）
3. **询问用户**：本周周报填入哪个分表？直接采用建议名、指定其他名称、或新建均可
4. **若选择新建**：脚本 `fill_weekly_report.py` 会自动从模板的干净分表复制结构并命名

将日期范围和用户选择的分表名称汇总确认后再继续。

---

## 第三步：收集周报内容

### 先对照上周计划

周报范围确定后，尝试定位上一周的周报：

1. 上周范围 = 本次周报范围往前推一周（`YYYYMMDD-YYYYMMDD`）
2. 在输出目录（profile 的 `output_dir` 或用户指定目录）查找文件名含上周范围的 `*工作周报*.md`
3. 没有则放宽为：日期范围早于本次、且最近的一份周报 md

找到后读取其"四、下周工作计划"，与用户素材一起对照收集：

- **一致**：素材与上周计划吻合 → 直接融合，作为"本周工作内容"
- **违和**：素材与上周计划相矛盾（方向调整、计划外的新工作、素材未提及计划事项）→ **明确询问用户以哪个为准**，不要自行取舍
- **未完成**：用户明确表示某条计划本周没做 → 如实处理（转入"问题与困难"或不写），**严禁编造"已完成"**

未找到上周周报 → 跳过对照，直接收集。

### 内容来源（让用户选择）

先询问用户用哪种来源再收集；来源可组合（如"git + 口头补漏"）。**当用户给出的材料是"项目路径/代码仓库"或提到"这周在 XX 项目写代码"时，先按下方 D 探测该项目是否为 git 仓库**——是则优先用 git 只读提取本周提交与 diff，再口头补齐 git 反映不出的部分。

- **A. 口头描述**：用户口述，你整理填入四个板块
- **B. 指定文件/笔记**：用户给出文件路径，你从中提取
- **C. 交互式问答**：你逐板块提问，用户回答
- **D. 项目 git 提交/diff（只读）**：用户给出项目路径，锁定本周日期，只读查询该项目本周提交与 diff 汇总为素材。不要求先提交——未提交的工作区改动同样读取，并按"进行中/未提交"如实标注。细则见下方专节
- **E. 已有日报/报工文本**：用户本周已按 git 法逐天做过日报/报工，把文本粘贴或给文件路径 → 按天聚合成周素材（与 D 二选一即可，避免重复查询）

### D. 项目 git 提交/diff 提取细则（只读）

**适用**：本周在某个（些）git 项目写了代码、用户能给出项目路径。多个项目逐个执行同一套查询，结果合并去重后再归纳。

**铁律——全程只读，绝不改动 git**：
- 只用 `rev-parse` / `log` / `show` / `diff` / `status` 等查询类命令
- 禁止 `add` / `commit` / `push` / `pull` / `merge` / `checkout` / `stash` / `reset` / `clean` / `rm` 等一切写操作，不改动工作区、暂存区、分支与远端
- 提交、推送等版本操作始终由用户本人完成；结束时提醒用户还有未提交改动时自行提交

**步骤**：
1. **探测仓库**：`git -C <项目路径> rev-parse --show-toplevel`。成功 → 以输出路径为仓库根（`git -C` 在子目录也能自动向上找到 .git，无需 cd）；失败或提示 not a git repository → 告知用户该路径不是 git 仓库，改用口述/文件来源，或请其给出正确路径，不要硬查。
2. **锁定时间范围**：第二步确定的周报范围 —— 周一 `00:00:00` ~ 周日 `23:59:59`（git 按 commit date 过滤，与作者日期可能不同）。补写上周周报则用上周范围；范围跨月/跨年时按日历日计算即可。
3. **确认作者**：优先取档案中的 `git_author`；没有则按 `profile.name` 匹配。拿不准时先列出该时间窗内的提交者（`git log --since=<周一> --until=<周日> --pretty=format:%an --all | sort -u`）请用户认领，不要猜。
4. **查提交（排除合并）**：
   ```
   git -C <路径> log --since="YYYY-MM-DD 00:00:00" --until="YYYY-MM-DD 23:59:59" \
       --no-merges --author="<作者>" --all \
       --pretty=format:"%h|%ad|%s" --date=format:"%Y-%m-%d %H:%M:%S"
   ```
   需要文件级改动概览时加 `--stat`，或对重点提交逐个 `git show --stat <hash>`；需要看具体实现/联调口径时用 `git show <hash>` 读 diff hunk。
5. **查未提交改动**：`git status --short`、`git diff --stat`、`git diff --cached --stat`。有未提交 → 纳入素材但统一标注"未提交/进行中"，并提醒用户这部分还没提交、周报中按进行中表述。
6. **噪音过滤与归并**：同一改动跨分支重复（如个人分支与 dev 上的同内容副本、同秒同主题的二次提交）只算一次；合并提交（merge）不计入工作内容；纯格式/注释微调与实质改动分开。最终归纳成"该周实质提交集合"，映射到 3~5 条任务。
7. **映射四个板块**：提交+diff 主要喂「一、本周工作内容」，每条写清：改了什么（功能/文件/接口）、怎么做的（技术方法、联调口径）、可验证结果（"已联调确认""已提交至 XX 分支/hash"）；「二、学习与收获」「三、问题与困难」git 往往反映不出，须就提交暴露的知识点、遗留 TODO、未决事项向用户提问补齐；「四、下周工作计划」结合未提交/进行中的部分与用户确认。
8. **诚实边界（必须向用户说明）**：git 只覆盖"已提交 + 工作区可见"的部分，覆盖不到——未及保存就下班的改动、放在别人分支的代码、需求沟通与联调过程、踩坑心得。**严禁仅凭 git 断言"这就是本周全部工作"，严禁编造未发生的完成项**（与下方质量要求一致）。
9. 提取结果整理成草稿 → 给用户核对增删 → 通过后再按下方质量要求自检，进入第四步。

### 四个板块

| 板块 | 格式 | 说明 |
|------|------|------|
| 一、本周工作内容 | 表格，序号+工作内容+完成情况 | 按任务逐条列出，每条约1-2行 |
| 二、学习与收获 | 编号列表，每条1-3句 | 新知识、新技能、新方法、新认识 |
| 三、问题与困难 | 编号列表，每条1-3句 | 困难、未解决问题、需协调事项 |
| 四、下周工作计划 | 编号列表，每条1句 | 下周拟开展的主要工作 |

### 内容质量要求（严格遵守）

这是周报的灵魂 —— **空洞的周报等于没写**。生成前逐条自检：

**禁止项（绝对不允许出现）：**
- ❌ "无" — 如果本周确实无事可写，写明原因（如"本周因XX培训未参与开发"）
- ❌ "正常""顺利" — 这类词单独出现等于废话，必须说明**怎么正常、怎么顺利**
- ❌ "完成了XX任务" — 太笼统，必须写清楚**怎么做的、用了什么方法、产出是什么**
- ❌ "学习了XX技术" — 必须写**学到了什么具体知识、能解决什么问题**
- ❌ "继续XX工作"（下周计划） — 必须写**下周具体要推进到什么程度、交付什么**

**每一项自问三个问题：**
1. 一个不了解我工作的人读完这条，能说清楚我做了什么吗？
2. 这条里有没有具体的技术名词、文件名、数据或时间节点？
3. 这条的「完成情况」是否描述了可验证的结果（如"已提交至XX"、"已部署验证通过"）？

**各板块数量建议：**
- 工作内容表格：3~5 条，少于 3 条说明本周产出不足，需要在学习与收获中补充
- 学习与收获：3~5 条
- 问题与困难：1~4 条（如实填写，没有困难也要写"无重大困难"，并简要说明原因）
- 下周计划：3~5 条

**条目数量灵活，模板预留行数不是上限：**
模板中任务表格通常预留 5 行、其余板块各预留 4 行（用户也可能自己改过预留数），这只是初始预留。实际条目可多可少：
- 内容多于预留行 → xlsx 生成时自动插入新行（保留样式与合并单元格）
- 内容少于预留行 → 自动删除空白行，末尾说明文字跟随上移

因此收集内容时**不要受"模板只有 4/5 行"限制**，按实际工作如实列出即可。

---

## 第四步：生成 .md 周报

### 文件命名

```
姓名工作周报(YYYYMMDD-YYYYMMDD)-指导老师.md
```

### 文件结构

使用以下精确模板：

```markdown
---
type: 周报
tags:
  - 周报
created: YYYY-MM-DD
---

# 姓名工作周报（YYYYMMDD-YYYYMMDD）-指导老师

> 实习生周报
> 姓名：[姓名]    部门：[部门]    岗位：[岗位]
> 指导老师：[指导老师]    入职时间：[入职时间]    填写日期：[填写日期]

---

## 一、本周工作内容（按任务逐条列出，说明工作内容、完成情况）

| 序号 | 工作内容 | 完成情况 |
|------|----------|----------|
| 1 | [内容] | [情况] |

---

## 二、学习与收获（本周学到的新知识、新技能、新方法，或对岗位/业务的新认识）

1、[内容]

---

## 三、问题与困难（工作中遇到的困难、尚未解决的问题，或需要指导老师/部门协调的事项）

1、[内容]

---

## 四、下周工作计划（逐条列出下周拟开展的主要工作）

1、[内容]
```

> 该模板同时是 xlsx 生成脚本的解析格式：信息区字段顺序、板块标题写法、表格列序、"1、"编号都不要改动，否则脚本无法解析。

### 生成后自检

生成 .md 后，**先对照第三步的质量要求逐项自检**，发现空洞条目立即要求用户补充，不要带着不合格内容进入 xlsx 生成环节。

自检通过后展示给用户审阅，允许修改后再进入第五步。

### 注意事项

- frontmatter 中 `type: 周报`，`created` 填填写日期
- wikilink 使用 `[[路径/文件名.md]]` 格式（含 `.md` 扩展名）
- 工作内容表格中，序号列、工作内容列（B+C 合并）、完成情况列（D+E+F 合并），与 xlsx 合并单元格对应

---

## 第五步：生成 .xlsx 周报

### 首选：自带脚本 + --from-md

`.md` 审阅通过后，直接交给脚本解析填写。md 中已含全部信息与内容，无需在命令行传 JSON（Windows cmd 下 JSON 引号转义极易出错）：

```bash
python scripts/fill_weekly_report.py \
  --template assets/实习生周报标准模板.xlsx \
  --from-md "<输出目录>/姓名工作周报(YYYYMMDD-YYYYMMDD)-指导老师.md" \
  --sheet "<分表名>" \
  --output "<输出目录>/姓名工作周报(YYYYMMDD-YYYYMMDD)-指导老师.xlsx"
```

脚本会自动：
1. 解析 md：信息区（姓名/部门/岗位/指导老师/入职时间/填写日期）与四个板块内容
2. 创建输出目录（不存在时）
3. 复制模板 → 定位或创建分表（新分表从干净分表复制结构）
4. 按表头文本动态定位四个板块，填写内容
5. 行数随内容增减：多了在板块末尾插入行（复制样式与合并单元格），少了删除空白行
6. 行高随内容自适应：内容超过两行时抬高行高，避免文字被截断

md 中的个别字段需要覆盖时，追加命令行参数即可（显式参数优先），如 `--fill-date "2026-09-11"`。完整参数见 `python scripts/fill_weekly_report.py --help`。

### 备用：命令行 JSON 参数

```bash
python scripts/fill_weekly_report.py \
  --template assets/实习生周报标准模板.xlsx \
  --output <输出路径>/姓名工作周报(YYYYMMDD-YYYYMMDD)-指导老师.xlsx \
  --name "姓名" \
  --department "部门" \
  --position "岗位" \
  --mentor "指导老师" \
  --entry-date "入职时间" \
  --fill-date "填写日期" \
  --sheet "8月第3周" \
  --tasks '[["内容1","完成情况1"],["内容2","完成情况2"]]' \
  --learnings '["收获1","收获2"]' \
  --problems '["问题1","问题2"]' \
  --plans '["计划1","计划2"]'
```

> JSON 参数为 bash 单引号写法；Windows cmd 下转义麻烦，仅建议在无法提供 md 文件时使用。

### 环境兜底顺序

1. **优先使用自带脚本** — 它为本模板定制（动态定位板块、行数增减、合并单元格维护、行高自适应），通用工具不了解这些约定
2. 无 Python / openpyxl 时，引导用户安装：
   ```
   pip install openpyxl
   # 或
   uv pip install openpyxl
   ```
3. 用户不想安装 → 退而求其次用 xlsx skill 或手动打开模板 xlsx 填写。注意：xlsx skill 不了解本模板的动态行数与合并单元格约定，填写后必须打开文件核对样式与行高

### xlsx 模板结构说明

模板位于 `assets/实习生周报标准模板.xlsx`，**仅含一个"模板"分表**（干净结构 + 示例占位），每周输出文件 = 模板副本 + 目标周分表。结构如下（条目行数为初始预留值，实际行数由脚本按内容自动调整）：

| 区域 | 内容 | 合并单元格 |
|------|------|------------|
| 标题行 | 标题"实习生周报" | A:F |
| 信息行 | 姓名(B) / 部门(D) / 岗位(F)；指导老师(B) / 入职时间(D) / 填写日期(F) | — |
| 一、本周工作内容 | 表头行 + 列头行（序号/工作内容/完成情况）+ 预留 5 行条目 | 条目行 B:C 合并、D:F 合并 |
| 二、学习与收获 | 表头行 + 预留 4 行条目 | 条目行 A:F 合并 |
| 三、问题与困难 | 表头行 + 预留 4 行条目 | 条目行 A:F 合并 |
| 四、下周工作计划 | 表头行 + 预留 4 行条目 | 条目行 A:F 合并 |
| 末尾 | 说明文字 | A:F 合并 |

脚本需处理的要点：
- 板块位置按表头文本动态识别，不依赖固定行号（用户改动模板预留行数也能正常工作）
- 合并单元格区域在写入前保留，写入内容后保持合并
- 分表名称不存在时，复制干净分表结构并重命名
- 条目行数随内容自动增减：多了在板块末尾插入行（复制样式与合并），少了删除空白行
- 行高随内容自适应（内容超过两行时抬高，避免截断）
- 日期格式使用 `yyyy/m/d`（与模板一致）

---

## 完成后

1. 告知用户两个文件的完整路径
2. 提醒用户："md 版为内容源，xlsx 用于正式提交，请检查后提交至指导老师"
3. 若提交要求 xlsx 仅含本周分表，提醒用户删除输出文件中的"模板"分表后再提交
4. 如果用户项目中有 MOC 主页，提醒更新周报索引表格

---

## 附：日报/报工速记（git 只读法）

周报之外，同一套 git 方法可按天输出"报工"（不发周报文件，聊天直接给 3~5 句文本；用户要求落盘再生成文件）。这也是把日报沉淀为周报素材的入口。

1. **触发**：用户说"报工""总结今天做了什么""写今天的日报"，并给出项目路径（不填默认当前工作目录）。
2. **锁定日期**：默认"今天"；用户指定某天则用那天（如 `2026-09-02 00:00:00 ~ 23:59:59`）。周报需要素材时，可对本周逐天各执行一次再聚合（即第三步来源 E），不需要周报时只输出当天即可。
3. **查询与提取**：完全复用上方"第三步 D 细则"的只读命令与铁律——当天作者提交（用 diff 核对内容）+ 工作区未提交改动；合并同类提交、排除 merge 与噪音。
4. **输出口径**：3~5 句即可，每句句式 = **做了什么（接口/任务）+ 怎么做的（技术方法/联调口径）+ 可验证结果（已联调/已提交 hash）**，参考句式："完成 XX 接口联调：按实测确认标准接口无返回值、以未抛异常视为成功，补齐 XX 字段并回传 XX"。当天工作全在工作区未提交时明确写"未提交"，**不冒充已完成**；提交缺失时注明"git 查不到，据口述补充"。
5. **沉淀**：报工文本可让用户留存，或转给周报生成时作为素材来源 E 按天聚合。
